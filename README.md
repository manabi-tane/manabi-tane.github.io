# 学びのたね

高校の教科を、原典にあたりながら自分の手で確かめる学習サイト。いまは**古典**（古文・漢文）が入っています。

古文の名文を**縦書き**で読み、語をクリックすると**品詞・活用の種類と活用形・意味・語法メモ**が引ける学習サイト。

**<https://manabi-tane.github.io/> で公開しています。**

## 収録

| 作品 | 章段 |
| --- | --- |
| 竹取物語 | かぐや姫の生ひ立ち |
| 伊勢物語 | 東下り（八橋）／筒井筒 |
| 枕草子 | 春はあけぼの／我をば思ふや |
| 源氏物語 | いづれの御時にか（桐壺） |
| 方丈記 | ゆく河の流れ |
| 平家物語 | 祇園精舎／扇の的 |
| 徒然草 | つれづれなるままに（序段）／仁和寺にある法師 |
| 史記（漢文） | 鴻門之会 |
| 落窪物語 | 日々にあらねど（少将の懸想文） |
| 玉勝間 | すべて田舎には（方言について） |

## 使い方

- 語をクリック／タップ → 左（スマホは下）のパネルに語釈が出ます。矢印キーで前後の語へ。
- **助動詞一覧**タブ → その章段の助動詞を出現順に並べ、クリックで本文へジャンプ。
- **品詞を強調** → 助動詞・動詞・形容詞などに帯を掛けて拾い読み。
- **敬語に圏点** → 敬語にゴマ点が付き、パネルに尊敬／謙譲／丁寧の別が出ます。
- **現代語訳** → 各段の左に訳の列が立ちます。
- **読み仮名** → 読みのある語にルビを振ります（漢文は既定でオン）。
- **要チェック** → 句法・助動詞・敬語・難読語・語法メモのある語に帯を掛けます。
- **頻出** → テストでねらわれやすい箇所に枠が付きます。語釈パネルの「頻出」タブで問いと答えを確認できます。
- **辞書・活用表** → 語をひらくと、語釈の下に単語帳の語義（その語が持つ他の意味も）と、文法書の形の活用表（現在の活用形の欄に色がつく）が出ます。練習のカード裏にも同じものが出ます。
- **拡大縮小** → 二本指ピンチ、Ctrl＋ホイール、⌘＋ / ⌘− / ⌘0。拡大中は一本指で紙を動かせます。
- **PDF** → 縦書きのまま高画質（ベクタ）で書き出せます。
- **★** → 章段をお気に入りに入れると、作品一覧の先頭に並びます。

## PDF に書き出す

本文ページの **PDF** ボタン（`assets/pdf.js`）。画面を画像に写し取るのではなく、印刷用の紙面を組み直します。

- **字はベクタのまま**入るので、拡大してもぼけず、PDF の中を検索・選択できます
- 縦書きのまま、紙の大きさに合わせて自動でページを分けます（測ってから組み直す二段構え）
- 用紙 A4／B5 の縦横、字の大きさ3段階、余白3段階
- **字間**（0〜0.5em）と**行間**（1.4〜7.0）はつまみで自由に決められます。行間はメモ用のよりどころとして「標準／広め／メモ用／たっぷり」の押しボタンつき。行の間に書き込めるだけ広げられます
- **背景色** 白／生成り／セピア／薄グレー／濃紺（濃紺は文字が白抜きになります）
- **段ごとに行を改める**（既定でオン）。白文・現代語訳もそれぞれ行頭から始まります
- 入れるものを選べます：現代語訳・読み仮名・敬語の圏点・品詞の色分け・句法や語法メモの印・白文（漢文）
- 開いたときの設定は画面の表示状態を引き継ぎます

書き出しはブラウザの印刷ダイアログ経由です。保存先で「PDFとして保存」を選んでください（iPad は共有 → プリント → ファイルに保存）。

## この形になる理由

`assets/why.js`。活用形は「下に何が続くか」と「係り結び」でほぼ決まるので、直後の語と語釈の記述から理由を自動で組み立てます。用言・助動詞 1,125 語のうち 1,015 語（90%）に出ます。

- 下に助動詞 → その助動詞の接続を `conj.js` の表から引く（「『ず』が未然形に接続するから未然形」）
- 下に助詞 → 接続助詞・終助詞の接続表から（「ば」は未然形なら仮定、已然形なら確定と書き分け）
- 下に補助動詞・用言 → 連用形／読点で切れれば連用中止法
- 下に体言・同格の格助詞 → 連体形
- 係り結び → **語釈に書いた「〜の結び」を根拠にする**。さかのぼって近い係助詞を拾う方式だと、結びが省略されている「か」などに後ろの連体形を片端からぶら下げてしまうため

語釈パネル・フラッシュカードの裏・文法問題の答え合わせに出ます。

## 辞書と活用表

- `assets/lexicon.js` … 見出し語ごとの語義。用言は基本形、それ以外は本文の表記がキー。表記のゆれは `LEXALIAS` で寄せる。
- `assets/conj.js` … 活用表。助動詞は表を直に持ち、用言は活用の種類と基本形から自動で組み立てる。
- **練習** → 章段ごとに4つのモード。
  - テスト対策（頻出ポイントの一問一答。全14章段で163問。`assets/spots.js` に手書き。お気に入りの5章段は各20問の定期テスト形式）
  - フラッシュカード（表に語と文脈、裏に品詞・活用・意味。「覚えた」で仕分け）
  - 単語クイズ（語→意味／意味→語／読み の四択10問。終わりに間違えた語の一覧）
  - 文法問題（句法／品詞／活用の種類と活用形／助動詞の説明／敬語の種類 の四択10問）
  - 出題の範囲を「すべての語／要チェックのみ」で切り替えられます

## 漢文の章段について

漢文（`genre:'漢文'`）は表示が変わります。

- **白文を書き下し文の右に置き、既定で表示**（読む順に並ぶ）
- **読み仮名を既定でオン**
- 語釈パネルに**句法タブ**が加わり、句法ごとにまとめて一覧・ジャンプできる（本文では金色の下線）
- 練習の文法問題に**句法の問題が毎回4問**入る

句法は本文データの語に `ku:'反語'` のように印を付けると拾われます。

お気に入りと学習の記録は、その端末のブラウザ（localStorage）にだけ保存されます。

## 章段を足すには

1. `texts/` に `<id>.js` を 1 つ置く（既存ファイルをまねる。`W()` が一語、`P()` が記号、`BR()` が改行）
2. `build.py` の `ORDER` に id を加える（省略すると末尾に並びます）
3. `python3 build.py` を実行 → ページと作品一覧が自動生成されます

## 構成

```
index.html            作品一覧（build.py が生成）
<id>.html             各章段のページ（build.py が生成）
assets/style.css      全ページ共通のスタイル
assets/kit.js         データ記述用のヘルパー W / P / K / BR
assets/reader.js      本文の組み立てと語釈パネル
assets/works.js       収録一覧（build.py が生成）
texts/<id>.js         本文＋品詞分解データ（手で書くのはここだけ）
build.py              ページ生成
開く.command          ビルドしてブラウザで開く
```

本文は著作権の切れた古典作品。品詞分解・現代語訳・語法メモは学校文法（古典文法）に沿って書き起こしたものです。


## 開きかた

- **`開く.command`** をダブルクリック → ブラウザで開きます（いちばん手軽）
- **`サーバーで開く.command`** をダブルクリック → 小さなサーバーが立ち上がり、
  同じ Wi-Fi の iPad などからも開けます（表示される LAN アドレス）。
  書体を確実に効かせたいときや、他の端末から使うときはこちら

どちらも実行前に `python3 build.py` が走るので、データを直したらそのまま反映されます。

## 公開

<https://manabi-tane.github.io/> で公開しています。GitHub Pages から配信しています。
公開用にビルドするときは `CACHE_BUST=1 python3 build.py` とすると、
読み込むファイルに内容ハッシュが付き、古いキャッシュが残りません。

## 構成（2026年9月〜）

```
index.html          教科の一覧（最上位）
privacy.html        プライバシーポリシー
terms.html          利用規約
cookies.html        Cookie とデータの扱い
404.html            見つからないとき
kobun/              古典
  index.html          作品一覧
  <id>.html           本文
  <id>-drill.html     練習
  texts/<id>.js       本文＋品詞分解データ
assets/             全教科で共通（style.css / *.js / fonts/）
docs/               法務ページの本文（build.py が枠を付ける）
```

教科を足すときは `build.py` の `SUBJECTS` に1行足し、同名のフォルダを作ります。

## 外部への通信はありません

書体（Zen Old Mincho／Zen Kaku Gothic New、SIL OFL 1.1）は Google Fonts から
配信を受けるのをやめ、`assets/fonts/` に部分集合ファイルごと置いて同一オリジンで
配っています。ページを開いても第三者のサーバーへは一切つながりません。

アクセス解析・広告・Cookie・フォーム・アカウントはいずれもありません。
端末に保存するのは、お気に入り（`kobun:favorites`）と覚えた語の記録
（`kobun:learned:<章段>`）だけです。

## アクセシビリティ

本文9ページ×ライト／ダークの18通りで、文字色と背景色の比が
WCAG 2.1 AA（小さい文字 4.5:1、大きい文字 3:1）を満たすことを機械的に確認しています。
名前のない操作要素はありません。本文ページの先頭に「本文へスキップ」を置いています。
